[package]
name = "entity"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
name = "entity"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
sea-orm = { version = "^2" }
sea-query = { version = "1" }

[lints.clippy]
enum_variant_names = "allow"