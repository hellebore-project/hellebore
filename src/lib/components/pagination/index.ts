import { type PaginationProps } from "./pagination-interface";
import { PaginationService } from "./pagination-service.svelte";
import { default as Pagination } from "./pagination.svelte";

export {
    type PaginationProps,
    PaginationService,
    Pagination,
    type PaginationProps as Props,
    PaginationService as Service,
    Pagination as Component,
};
